apiVersion: v1
kind: Service
metadata:
  name: cloudcostops-postgres
  namespace: cloudcostops

spec:
  type: ClusterIP

  selector:
    app: cloudcostops-postgres

  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      protocol: TCP