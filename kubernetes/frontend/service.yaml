apiVersion: v1
kind: Service
metadata:
  name: cloudcostops-frontend
  namespace: cloudcostops

spec:
  type: ClusterIP

  selector:
    app: cloudcostops-frontend

  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP